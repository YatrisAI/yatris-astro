---
'create-yatris': patch
'@yatris/astro': patch
---

Reference-site findings (checkpoint 1): generated repositories start on `main` whatever the local Git default is, and the sign-in instructions now include the steps each client needs first (approving the project server in Claude Code, trusting the project in Codex).
