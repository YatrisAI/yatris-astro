import type { Alpine } from 'alpinejs';

// Runs before Alpine starts (wired in astro.config.mjs). Register plugins,
// directives, stores and named `Alpine.data()` components here. Install
// plugins as pinned npm dependencies; never load them from a CDN.
export default (_Alpine: Alpine) => {};
